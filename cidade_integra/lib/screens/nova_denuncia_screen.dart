import 'package:flutter/material.dart';

class NovaDenunciaScreen extends StatelessWidget {
  const NovaDenunciaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Nova Denúncia', style: TextStyle(fontSize: 24)),
    );
  }
}
